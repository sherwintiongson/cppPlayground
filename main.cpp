#include <iostream>
#include <windows.h>

#include "arrays_vectors_test.hpp"

int main() 
{
    uint16_t eachElement = 0;
    test_arrays_vectors();

    // Delay to avoid overwhelming VSCode
    Sleep(300);
}